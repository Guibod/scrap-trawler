import React from "react";
import { HeroUIProvider } from "@heroui/system"

const PlasmoRoot = () => {
  return (
    <HeroUIProvider>
      Plasmo root
    </HeroUIProvider>
  );
};

export default PlasmoRoot;