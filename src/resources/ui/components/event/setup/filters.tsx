import { Card, CardHeader, CardBody } from "@heroui/card";

const SetupFilters = () => {
  return (
    <Card>
      <CardHeader title="Filter and duplicates" />
      <CardBody>Filtering UI will go here</CardBody>
    </Card>
  );
};

export default SetupFilters;